// This component will create a vote button that reloads the page with new audio files asking the same question: "which sound is more _?"
import Link from "next/link";

export function VoteButton() {
  return <Link href="#"></Link>;
}
